import * as React from "react";

export interface IVerticalMenuProps {}

export default function VerticalMenu(props: IVerticalMenuProps) {
  return <div>This is vertical menu displayed when user/admin loggs in</div>;
}
